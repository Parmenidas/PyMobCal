In this folder we compare the fcoord functions that performs the first reading of the file.
In this case, we do not need to make the fortran function a python module.
We just run both codes independently and then compare the outputs.